{
    "actorOutputSchemaVersion": 1,
    "properties": {
        "video_url": {
            "type": "string",
            "title": "Video URL",
            "description": "The original URL of the YouTube video.",
            "template": "{{video_url}}"
        },
        "title": {
            "type": "string",
            "title": "Title",
            "description": "The title of the YouTube video.",
            "template": "{{title}}"
        },
        "uploader": {
            "type": "string",
            "title": "Uploader",
            "description": "The uploader of the YouTube video.",
            "template": "{{uploader}}"
        },
        "duration": {
            "type": "string",
            "title": "Duration (s)",
            "description": "The duration of the video in seconds.",
            "template": "{{duration}}"
        },
        "download_url": {
            "type": "string",
            "title": "Download Link",
            "description": "Direct link to download the video file.",
            "template": "{{download_url}}"
        },
        "error": {
            "type": "string",
            "title": "Error",
            "description": "Error message if the download failed.",
            "template": "{{error}}"
        }
    }
}
